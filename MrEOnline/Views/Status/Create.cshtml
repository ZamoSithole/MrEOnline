@model MrE.Models.Entities.Status

@{
    ViewBag.Title = "Add Status";
}


<div class="card">
    <div class="card-header">@ViewBag.Title</div>
    <div class="card-body">
        @using (Html.BeginForm())
        {
           @Html.Partial("_Form")
        }
    </div>
</div>

